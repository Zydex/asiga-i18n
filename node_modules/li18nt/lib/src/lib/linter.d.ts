import { JSONObject, Li18ntOptions, PartialLi18ntResult } from '@types';
export declare const version: string;
/**
 * Lints objects using the given configuration.
 * @param conf
 * @param objects
 */
export declare const lint: <T extends Partial<Li18ntOptions>>(conf: T, objects: JSONObject[]) => PartialLi18ntResult<T>;
