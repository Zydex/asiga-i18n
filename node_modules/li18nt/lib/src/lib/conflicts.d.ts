import { JSONObject, PropertyPath } from '@types';
export interface Conflict {
    missing: PropertyPath[];
    conflicts: PropertyPath[];
}
export declare type Conflicts = Conflict[];
/**
 * Finds the difference between given objects
 * @param objects
 */
export declare const conflicts: (objects: JSONObject[]) => Conflict[];
