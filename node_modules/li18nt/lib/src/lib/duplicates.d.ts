import { JSONObject, PropertyPath } from '@types';
export interface DuplicatesConfig {
    ignore?: (PropertyPath | string)[];
}
export declare type Duplicates = Map<string, PropertyPath[]>;
/**
 * Finds duplicate keys in the given object.
 * @param object
 * @param conf Optional configuration
 */
export declare const duplicates: (object: JSONObject, conf?: DuplicatesConfig) => Duplicates;
