import { JSONObject } from '@types';
export declare type Indentation = 'tab' | number;
export interface PrettifyOptions {
    indent?: Indentation;
}
/**
 * Sorts an object by its keys, returns a string as ordering properties manually
 * is slow and we can't rely on their order after inserting them.
 * @param obj
 * @param space
 */
export declare const prettify: (obj: JSONObject, { indent }: PrettifyOptions) => string;
